export type Post = {
  slug: string
  title: string
  description: string
  date: string
  tags: string[]
}

export type CaseStudy = {
  slug: string
  title: string
  description: string
  date: string
  tags: string[]
}
